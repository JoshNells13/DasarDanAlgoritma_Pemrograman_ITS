#include <stdio.h>


int main(){
    // int a, b, c, d;  
    // a = 11;  
    // b = 24;  
    // c = 11;  
    // e = ((a == c) && (b > a));               // 1 (TRUE)  
    // f = ((a >= b) || (a < c));               // 0 (FALSE)  
    // g = ((b == b) || (b > c)) && (c == a);   // 1 (TRUE) 


    // printf("e: %d\n", e);
    // printf("f: %d\n", f);
    // printf("g: %d\n", g);
}  

